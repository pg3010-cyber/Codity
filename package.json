{
  "name": "conveyor",
  "version": "1.0.0",
  "description": "Conveyor — a distributed job scheduling platform with queues, workers, retries and a live dashboard",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "worker": "node worker/index.js",
    "seed": "node scripts/seed.js",
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.10.0",
    "cron-parser": "^4.9.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  }
}
